# Уведомление о статусе созданной вакансии

## Описание изменений

Добавлено красивое уведомление с отображением статуса вакансии после её создания. Пользователь видит название созданной вакансии и её текущий статус (ACTIVE, PAUSED, CLOSED).

## Изменения в файлах

### 1. `src/app/jobs/create/page.tsx`
- **Добавлено состояние**: `showSuccessMessage` и `createdJobInfo` для управления уведомлением
- **Обновлена функция handleSubmit**: сохранение информации о созданной вакансии
- **Добавлена функция getStatusLabel**: перевод статусов на русский язык
- **Добавлен компонент уведомления**: модальное окно с информацией о статусе
- **Автоматический редирект**: через 3 секунды после создания

### 2. `src/app/jobs/create/create.module.css`
- **Добавлены стили уведомления**: `.successOverlay`, `.successMessage`
- **Анимации**: плавное появление и исчезновение
- **Стили статусов**: разные цвета для ACTIVE, PAUSED, CLOSED
- **Адаптивный дизайн**: корректное отображение на мобильных устройствах

## Функциональность

### Уведомление о создании:
- ✅ **Красивое модальное окно** с анимацией появления
- ✅ **Название вакансии** в кавычках
- ✅ **Статус с цветовой индикацией**:
  - 🟢 ACTIVE (Активна) - зеленый цвет
  - 🟡 PAUSED (Приостановлена) - желтый цвет  
  - 🔴 CLOSED (Закрыта) - красный цвет
- ✅ **Автоматический редирект** через 3 секунды
- ✅ **Адаптивный дизайн** для всех устройств

### Анимации:
- **Fade In**: плавное появление overlay
- **Slide In**: анимация появления модального окна
- **Responsive**: адаптация под размер экрана

## Структура уведомления

```jsx
{showSuccessMessage && createdJobInfo && (
  <div className={styles.successOverlay}>
    <div className={styles.successMessage}>
      <div className={styles.successIcon}>
        {/* Иконка успеха */}
      </div>
      <h2>Вакансия создана!</h2>
      <p>"{createdJobInfo.title}"</p>
      <div className={styles.successStatus}>
        <span>Статус:</span>
        <span className={statusClass}>
          {getStatusLabel(createdJobInfo.status)}
        </span>
      </div>
      <p>Перенаправление через 3 секунды...</p>
    </div>
  </div>
)}
```

## Цветовая схема статусов

### ACTIVE (Активна):
- Фон: `#dcfce7` (светло-зеленый)
- Текст: `#166534` (темно-зеленый)
- Граница: `#bbf7d0` (зеленая)

### PAUSED (Приостановлена):
- Фон: `#fef3c7` (светло-желтый)
- Текст: `#92400e` (темно-желтый)
- Граница: `#fde68a` (желтая)

### CLOSED (Закрыта):
- Фон: `#fee2e2` (светло-красный)
- Текст: `#dc2626` (темно-красный)
- Граница: `#fecaca` (красная)

## Пользовательский опыт

### До изменений:
- ❌ Простой редирект без уведомления
- ❌ Пользователь не знал статус вакансии
- ❌ Нет подтверждения успешного создания

### После изменений:
- ✅ Красивое уведомление с подтверждением
- ✅ Отображение статуса вакансии
- ✅ Понятная цветовая индикация
- ✅ Автоматический переход на страницу вакансий
- ✅ Профессиональный внешний вид

## Технические детали

### Состояние компонента:
```typescript
const [showSuccessMessage, setShowSuccessMessage] = useState(false);
const [createdJobInfo, setCreatedJobInfo] = useState<{
  title: string, 
  status: string
} | null>(null);
```

### Логика отображения статуса:
```typescript
const getStatusLabel = (status: string) => {
  switch (status) {
    case 'ACTIVE': return 'Активна';
    case 'PAUSED': return 'Приостановлена';
    case 'CLOSED': return 'Закрыта';
    default: return status;
  }
};
```

### Автоматический редирект:
```typescript
setTimeout(() => {
  router.push('/jobs');
}, 3000);
```

## Совместимость

- ✅ Все современные браузеры
- ✅ Мобильные устройства
- ✅ Планшеты и десктопы
- ✅ Сохранение существующего функционала
- ✅ Отсутствие ошибок линтера
